// ----- << External Library >> ----- //
#include <string>
#include <vector>

// ----- << Function Prototype >> ----- //
static std::vector<std::string> getTokens(std::string command, char delimiter);
static void clearConsole();
void processMultiplexerCommands();